# 2026-06-19 贵港旅游助手 收藏/打卡功能开发

## 背景
昨天凌晨在 V8 修复 cache 问题后开始开发打卡功能，已创建 `checkin.js`。
今天继续完成实现。

## 完成项

### spot_detail.html
- **底部操作栏**：添加「打卡」按钮（在收藏和AI讲解之间）
- 打卡按钮支持三种状态循环：未打卡→想去(👀)→去过(✅)→未打卡
- 按钮样式对应变色：`checked-want`（橙色）、`checked-visited`（绿色）
- 页面加载时自动同步打卡状态

### index.html
- **收藏面板改 Tab 结构**：分为「❤️ 收藏」和「📍 打卡」两个 Tab
- **打卡 Tab**：按状态分组显示（想去/去过），每组显示数量
- **景点列表**：每个景点卡片右侧新增 ♡/❤️ 收藏按钮，点击可直接切换收藏
- 新增 `toggleListFav()` 函数处理列表收藏交互

### spot_detail.css
- 添加 `.checkin-btn` 三种状态样式（default/want/visited）

## 涉及文件
- `templates/index.html` - 收藏面板改 Tab + 列表收藏按钮
- `templates/spot_detail.html` - 底部打卡按钮 + JS
- `static/css/spot_detail.css` - 打卡按钮状态样式
- `static/js/checkin.js` - 已存在，无需修改
